{
  "name": "health_check_functional",
  "description": "Verifies that the /health endpoint returns a healthy status.",
  "test_type": "functional",
  "input": "POST /health with no payload",
  "output": "JSON response with success=True and status='ok'",
  "success_criteria": [
    "HTTP status code is 200",
    "response.success is True",
    "response.status == 'ok'"
  ],
  "error_scenarios": [
    "Unexpected server error"
  ]
}